# 10 - 配置管理

## 概述

本文档描述配置管理功能的实现。负责读取、写入和管理应用配置。

## 开发顺序

**优先级：P1（重要）**  
**预计时间：1天**  
**前置依赖：03-工具函数实现**

## 文件位置

- `src/services/config.ts` - 配置服务

---

## 10.1 配置文件位置

### 默认位置

| 平台 | 路径 |
|------|------|
| Windows | `%USERPROFILE%\.codex-session-viewer\config.json` |
| macOS | `~/.codex-session-viewer/config.json` |
| Linux | `~/.codex-session-viewer/config.json` |

### 配置目录结构

```
~/.codex-session-viewer/
├── config.json          # 主配置文件
├── search-history.json  # 搜索历史
└── themes/              # 自定义主题
    └── custom.json
```

---

## 10.2 配置项

### 完整配置

```typescript
interface Config {
  // 基本配置
  sessionsPath: string;          // Session 文件路径
  defaultExportFormat: ExportFormat; // 默认导出格式
  maxResults: number;            // 最大结果数
  
  // 显示配置
  theme: 'default' | 'dark' | 'light' | 'colorful'; // 主题
  dateFormat: string;            // 日期格式
  showTimestamps: boolean;       // 显示时间戳
  highlightMatches: boolean;     // 高亮匹配
  language: 'zh' | 'en';        // 语言
  
  // 高级配置
  cacheEnabled: boolean;         // 启用缓存
  cacheTTL: number;              // 缓存过期时间（毫秒）
  maxFileSize: number;           // 最大文件大小（字节）
  concurrentParsing: number;     // 并发解析数
}
```

### 默认配置

```typescript
const DEFAULT_CONFIG: Config = {
  sessionsPath: '~/.codex/sessions',
  defaultExportFormat: 'json',
  maxResults: 50,
  theme: 'default',
  dateFormat: 'yyyy-MM-dd HH:mm',
  showTimestamps: true,
  highlightMatches: true,
  language: 'zh',
  cacheEnabled: true,
  cacheTTL: 3600000, // 1 hour
  maxFileSize: 104857600, // 100MB
  concurrentParsing: 5,
};
```

---

## 10.3 ConfigService 类

### 实现

```typescript
import fs from 'fs-extra';
import path from 'path';
import os from 'os';
import { Config, ExportFormat } from '@/types/session';
import { createLogger } from '@/utils/logger';

export class ConfigService {
  private configPath: string;
  private configDir: string;
  private config: Config | null = null;
  private logger = createLogger('ConfigService');

  constructor() {
    this.configDir = this.getConfigDir();
    this.configPath = path.join(this.configDir, 'config.json');
  }

  /**
   * 获取配置目录
   */
  private getConfigDir(): string {
    const homeDir = os.homedir();
    return path.join(homeDir, '.codex-session-viewer');
  }

  /**
   * 获取配置
   */
  async getConfig(): Promise<Config> {
    if (this.config) {
      return this.config;
    }

    try {
      // 确保配置目录存在
      await fs.ensureDir(this.configDir);

      // 读取配置文件
      const exists = await fs.pathExists(this.configPath);
      if (exists) {
        const content = await fs.readFile(this.configPath, 'utf-8');
        const savedConfig = JSON.parse(content);
        
        // 合并默认配置和保存的配置
        this.config = {
          ...DEFAULT_CONFIG,
          ...savedConfig,
        };
      } else {
        // 使用默认配置
        this.config = { ...DEFAULT_CONFIG };
        await this.saveConfig(this.config);
      }

      return this.config;
    } catch (error) {
      this.logger.error('Failed to load config:', error);
      return { ...DEFAULT_CONFIG };
    }
  }

  /**
   * 保存配置
   */
  async saveConfig(config: Config): Promise<void> {
    try {
      await fs.ensureDir(this.configDir);
      await fs.writeFile(
        this.configPath,
        JSON.stringify(config, null, 2),
        'utf-8'
      );
      this.config = config;
      this.logger.info('Config saved successfully');
    } catch (error) {
      this.logger.error('Failed to save config:', error);
      throw error;
    }
  }

  /**
   * 更新配置
   */
  async updateConfig(updates: Partial<Config>): Promise<Config> {
    const currentConfig = await this.getConfig();
    const newConfig = {
      ...currentConfig,
      ...updates,
    };
    
    await this.saveConfig(newConfig);
    return newConfig;
  }

  /**
   * 重置配置
   */
  async resetConfig(): Promise<Config> {
    const defaultConfig = { ...DEFAULT_CONFIG };
    await this.saveConfig(defaultConfig);
    return defaultConfig;
  }

  /**
   * 获取单个配置项
   */
  async get<K extends keyof Config>(key: K): Promise<Config[K]> {
    const config = await this.getConfig();
    return config[key];
  }

  /**
   * 设置单个配置项
   */
  async set<K extends keyof Config>(key: K, value: Config[K]): Promise<void> {
    await this.updateConfig({ [key]: value } as Partial<Config>);
  }

  /**
   * 获取配置文件路径
   */
  getConfigPath(): string {
    return this.configPath;
  }

  /**
   * 获取配置目录路径
   */
  getConfigDir(): string {
    return this.configDir;
  }

  /**
   * 检查配置文件是否存在
   */
  async configExists(): Promise<boolean> {
    return fs.pathExists(this.configPath);
  }

  /**
   * 导出配置
   */
  async exportConfig(): Promise<string> {
    const config = await this.getConfig();
    return JSON.stringify(config, null, 2);
  }

  /**
   * 导入配置
   */
  async importConfig(configJson: string): Promise<Config> {
    try {
      const importedConfig = JSON.parse(configJson);
      
      // 验证配置
      this.validateConfig(importedConfig);
      
      // 合并配置
      const currentConfig = await this.getConfig();
      const newConfig = {
        ...currentConfig,
        ...importedConfig,
      };
      
      await this.saveConfig(newConfig);
      return newConfig;
    } catch (error) {
      this.logger.error('Failed to import config:', error);
      throw error;
    }
  }

  /**
   * 验证配置
   */
  private validateConfig(config: any): void {
    // 验证必需字段
    if (config.sessionsPath && typeof config.sessionsPath !== 'string') {
      throw new Error('Invalid sessionsPath');
    }

    if (config.maxResults && (typeof config.maxResults !== 'number' || config.maxResults < 1)) {
      throw new Error('Invalid maxResults');
    }

    if (config.theme && !['default', 'dark', 'light', 'colorful'].includes(config.theme)) {
      throw new Error('Invalid theme');
    }

    if (config.language && !['zh', 'en'].includes(config.language)) {
      throw new Error('Invalid language');
    }

    if (config.defaultExportFormat && !['json', 'text', 'markdown', 'csv'].includes(config.defaultExportFormat)) {
      throw new Error('Invalid defaultExportFormat');
    }
  }

  /**
   * 获取搜索历史文件路径
   */
  getSearchHistoryPath(): string {
    return path.join(this.configDir, 'search-history.json');
  }

  /**
   * 获取主题目录路径
   */
  getThemesDir(): string {
    return path.join(this.configDir, 'themes');
  }
}

// 导出默认配置
export const DEFAULT_CONFIG: Config = {
  sessionsPath: '~/.codex/sessions',
  defaultExportFormat: 'json',
  maxResults: 50,
  theme: 'default',
  dateFormat: 'yyyy-MM-dd HH:mm',
  showTimestamps: true,
  highlightMatches: true,
  language: 'zh',
  cacheEnabled: true,
  cacheTTL: 3600000,
  maxFileSize: 104857600,
  concurrentParsing: 5,
};
```

---

## 10.4 使用示例

### 获取配置

```typescript
import { ConfigService } from '@/services/config';

const configService = new ConfigService();

// 获取完整配置
const config = await configService.getConfig();
console.log(config);

// 获取单个配置项
const theme = await configService.get('theme');
console.log(theme); // 'default'

const language = await configService.get('language');
console.log(language); // 'zh'
```

### 更新配置

```typescript
import { ConfigService } from '@/services/config';

const configService = new ConfigService();

// 更新单个配置项
await configService.set('theme', 'dark');
await configService.set('language', 'en');

// 更新多个配置项
await configService.updateConfig({
  theme: 'dark',
  language: 'en',
  maxResults: 100,
});

// 获取更新后的配置
const config = await configService.getConfig();
console.log(config.theme); // 'dark'
console.log(config.language); // 'en'
```

### 重置配置

```typescript
import { ConfigService } from '@/services/config';

const configService = new ConfigService();

// 重置为默认配置
const defaultConfig = await configService.resetConfig();
console.log(defaultConfig);
```

### 导入/导出配置

```typescript
import { ConfigService } from '@/services/config';

const configService = new ConfigService();

// 导出配置
const configJson = await configService.exportConfig();
console.log(configJson);

// 导入配置
const importedConfig = await configService.importConfig(configJson);
console.log(importedConfig);
```

---

## 10.5 配置文件示例

### 基本配置

```json
{
  "sessionsPath": "~/.codex/sessions",
  "defaultExportFormat": "json",
  "maxResults": 50,
  "theme": "default",
  "dateFormat": "yyyy-MM-dd HH:mm",
  "showTimestamps": true,
  "highlightMatches": true,
  "language": "zh"
}
```

### 高级配置

```json
{
  "sessionsPath": "~/.codex/sessions",
  "defaultExportFormat": "markdown",
  "maxResults": 100,
  "theme": "dark",
  "dateFormat": "yyyy-MM-dd HH:mm:ss",
  "showTimestamps": true,
  "highlightMatches": true,
  "language": "en",
  "cacheEnabled": true,
  "cacheTTL": 7200000,
  "maxFileSize": 209715200,
  "concurrentParsing": 10
}
```

---

## 10.6 环境变量支持

### 环境变量

| 变量 | 说明 | 示例 |
|------|------|------|
| CODEX_SESSIONS_PATH | Session 路径 | `~/.codex/sessions` |
| CODEX_THEME | 主题 | `dark` |
| CODEX_LANGUAGE | 语言 | `zh` |
| CODEX_MAX_RESULTS | 最大结果数 | `50` |
| CODEX_EXPORT_FORMAT | 默认导出格式 | `json` |

### 优先级

配置优先级（从高到低）：
1. 命令行参数
2. 环境变量
3. 配置文件
4. 默认值

---

## 10.7 错误处理

### 常见错误

1. **配置目录不存在**
   - 处理：自动创建目录
   - 日志：info 级别

2. **配置文件格式错误**
   - 处理：使用默认配置
   - 日志：warn 级别

3. **配置文件权限不足**
   - 处理：使用默认配置
   - 日志：error 级别

4. **配置验证失败**
   - 处理：抛出验证错误
   - 日志：error 级别

### 错误处理示例

```typescript
import { ConfigService } from '@/services/config';

const configService = new ConfigService();

try {
  const config = await configService.getConfig();
  // 使用配置
} catch (error) {
  if (error.message.includes('permission')) {
    console.error('配置文件权限不足');
  } else if (error.message.includes('invalid')) {
    console.error('配置文件格式错误');
  } else {
    console.error('加载配置失败:', error);
  }
  
  // 使用默认配置
  const defaultConfig = await configService.resetConfig();
}
```

---

## 10.8 配置迁移

### 版本迁移

```typescript
interface ConfigVersion {
  version: string;
  migrate: (config: any) => any;
}

const migrations: ConfigVersion[] = [
  {
    version: '1.0.0',
    migrate: (config) => ({
      ...config,
      version: '1.0.0',
    }),
  },
  {
    version: '1.1.0',
    migrate: (config) => ({
      ...config,
      version: '1.1.0',
      cacheEnabled: config.cacheEnabled ?? true,
      cacheTTL: config.cacheTTL ?? 3600000,
    }),
  },
];

async function migrateConfig(config: any): Promise<Config> {
  let migratedConfig = { ...config };
  
  for (const migration of migrations) {
    if (!migratedConfig.version || migratedConfig.version < migration.version) {
      migratedConfig = migration.migrate(migratedConfig);
    }
  }
  
  return migratedConfig;
}
```

---

## 完成标准

- [x] ConfigService 类已实现
- [x] getConfig 方法工作正常
- [x] saveConfig 方法工作正常
- [x] updateConfig 方法工作正常
- [x] resetConfig 方法工作正常
- [x] 导入/导出功能已实现
- [x] 配置验证已实现
- [x] 环境变量支持已实现
- [x] 错误处理完善
- [x] 有使用示例
- [x] 无 TypeScript 编译错误

## 下一步

完成本文档后，继续进行 [11-测试编写](./11-测试编写.md)。
