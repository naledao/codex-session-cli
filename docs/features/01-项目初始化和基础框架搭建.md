# 01 - 项目初始化和基础框架搭建

## 概述

本文档描述如何初始化 Codex Session Viewer 项目，并搭建基础框架。这是项目开发的第一步，为后续功能开发奠定基础。

## 开发顺序

**优先级：P0（必须）**  
**预计时间：1-2天**  
**前置依赖：无**

## 任务清单

### 1.1 初始化 npm 项目

#### 目标
创建项目根目录并初始化 npm 项目配置。

#### 步骤

1. 创建项目目录
```bash
mkdir codex-session-viewer
cd codex-session-viewer
```

2. 初始化 npm 项目
```bash
npm init -y
```

3. 配置 `package.json`
```json
{
  "name": "codex-session-viewer",
  "version": "1.0.0",
  "description": "一个跨平台的终端工具，用于查看OpenAI Codex CLI在当前目录下的历史session记录",
  "main": "dist/index.js",
  "bin": {
    "codex-sessions": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix",
    "format": "prettier --write src/**/*.{ts,tsx,json}",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "prepare": "husky install"
  },
  "keywords": [
    "codex",
    "cli",
    "session",
    "viewer",
    "terminal",
    "tui",
    "openai"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
```

#### 验证
- [x] `package.json` 文件已创建
- [x] 配置项完整且正确

---

### 1.2 安装依赖

#### 目标
安装项目所需的所有依赖包。

#### 核心依赖
```bash
npm install chalk commander ink ink-select-input ink-spinner ink-text-input react date-fns fs-extra jsonstream ora cli-table3
```

#### 开发依赖
```bash
npm install -D @types/node @types/react @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint eslint-config-prettier eslint-plugin-react eslint-plugin-react-hooks husky lint-staged prettier tsup typescript vitest
```

#### 依赖说明

| 依赖 | 用途 |
|------|------|
| chalk | 终端颜色输出 |
| commander | 命令行参数解析 |
| ink | React for CLI - TUI框架 |
| react | React 核心库 |
| date-fns | 日期格式化和处理 |
| fs-extra | 增强的文件系统操作 |
| jsonstream | 流式JSONL解析 |
| ora | 终端进度指示器 |
| cli-table3 | 终端表格渲染 |

#### 验证
- [x] `node_modules` 目录已创建
- [x] `package-lock.json` 文件已生成
- [x] 所有依赖安装成功

---

### 1.3 配置 TypeScript

#### 目标
创建 TypeScript 配置文件，设置编译选项。

#### 创建 `tsconfig.json`
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "typeRoots": ["./node_modules/@types", "./src/types"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests", "**/*.test.ts", "**/*.test.tsx"]
}
```

#### 关键配置说明

| 配置项 | 说明 |
|--------|------|
| target | 编译目标为 ES2022 |
| module | 使用 ESNext 模块系统 |
| jsx | 使用 React 17+ 的 JSX 转换 |
| strict | 启用严格模式 |
| paths | 配置路径别名 `@/*` |

#### 验证
- [x] `tsconfig.json` 文件已创建
- [x] TypeScript 编译器可以正常工作

---

### 1.4 搭建项目结构

#### 目标
创建项目的目录结构。

#### 目录结构
```
codex-session-viewer/
├── src/
│   ├── commands/           # 命令实现
│   ├── components/         # TUI组件
│   ├── services/           # 业务逻辑服务
│   ├── utils/              # 工具函数
│   └── types/              # TypeScript类型定义
├── tests/
│   ├── unit/               # 单元测试
│   └── integration/        # 集成测试
└── docs/
    └── features/           # 功能文档
```

#### 创建目录
```bash
mkdir -p src/commands
mkdir -p src/components
mkdir -p src/services
mkdir -p src/utils
mkdir -p src/types
mkdir -p tests/unit
mkdir -p tests/integration
mkdir -p docs/features
```

#### 验证
- [x] 所有目录已创建
- [x] 目录结构正确

---

### 1.5 配置代码质量工具

#### 目标
配置 ESLint、Prettier 和 EditorConfig。

#### 创建 `.eslintrc.js`
```javascript
module.exports = {
  parser: '@typescript-eslint/parser',
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react-hooks/recommended',
    'prettier',
  ],
  parserOptions: {
    ecmaVersion: 2022,
    sourceType: 'module',
    ecmaFeatures: {
      jsx: true,
    },
  },
  settings: {
    react: {
      version: 'detect',
    },
  },
  env: {
    node: true,
    es2022: true,
  },
  rules: {
    '@typescript-eslint/explicit-function-return-type': 'off',
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    '@typescript-eslint/no-explicit-any': 'warn',
    '@typescript-eslint/no-empty-function': 'warn',
    'react/react-in-jsx-scope': 'off',
    'react/prop-types': 'off',
    'no-console': ['warn', { allow: ['warn', 'error'] }],
    'prefer-const': 'error',
    'no-var': 'error',
    'eqeqeq': ['error', 'always'],
    'no-multiple-empty-lines': ['error', { max: 1 }],
    'no-trailing-spaces': 'error',
    'eol-last': 'error',
    'comma-dangle': ['error', 'always-multiline'],
    'semi': ['error', 'always'],
    'quotes': ['error', 'single', { avoidEscape: true }],
  },
  ignorePatterns: ['dist', 'node_modules', 'coverage', '*.js'],
};
```

#### 创建 `.prettierrc`
```json
{
  "semi": true,
  "trailingComma": "all",
  "singleQuote": true,
  "printWidth": 100,
  "tabWidth": 2,
  "useTabs": false,
  "endOfLine": "lf",
  "bracketSpacing": true,
  "jsxBracketSameLine": false,
  "arrowParens": "avoid",
  "proseWrap": "preserve",
  "htmlWhitespaceSensitivity": "css",
  "vueIndentScriptAndStyle": false,
  "singleAttributePerLine": false
}
```

#### 创建 `.editorconfig`
```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

[*.md]
trim_trailing_whitespace = false

[*.{js,jsx,ts,tsx}]
indent_size = 2

[*.{json,yml,yaml}]
indent_size = 2
```

#### 验证
- [x] `.eslintrc.js` 文件已创建
- [x] `.prettierrc` 文件已创建
- [x] `.editorconfig` 文件已创建
- [x] ESLint 可以正常运行
- [x] Prettier 可以正常运行

---

### 1.6 配置构建工具

#### 目标
配置 tsup 构建工具。

#### 创建 `tsup.config.ts`
```typescript
import { defineConfig } from 'tsup';

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['cjs', 'esm'],
  dts: true,
  splitting: false,
  sourcemap: true,
  clean: true,
  treeshake: true,
  minify: false,
  target: 'node18',
  outDir: 'dist',
  outExtension({ format }) {
    if (format === 'cjs') return { js: '.js' };
    if (format === 'esm') return { js: '.mjs' };
    return { js: '.js' };
  },
  esbuildOptions(options) {
    options.jsx = 'automatic';
    options.jsxImportSource = 'react';
    options.define = {
      'process.env.NODE_ENV': JSON.stringify(process.env.NODE_ENV || 'development'),
    };
  },
  external: [
    'react',
    'react-dom',
    'ink',
    'chalk',
    'commander',
    'fs-extra',
    'date-fns',
    'cli-table3',
    'ora',
    'jsonstream',
  ],
});
```

#### 验证
- [x] `tsup.config.ts` 文件已创建
- [x] 构建命令可以正常运行

---

### 1.7 配置测试框架

#### 目标
配置 Vitest 测试框架。

#### 创建 `vitest.config.ts`
```typescript
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['tests/**/*.{test,spec}.{js,mjs,cjs,ts,mts,cts,jsx,tsx}'],
    exclude: ['node_modules', 'dist', '.idea', '.git', '.cache'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      reportsDirectory: './coverage',
      include: ['src/**/*.{ts,tsx}'],
      exclude: [
        'src/**/*.{test,spec}.{ts,tsx}',
        'src/**/index.ts',
        'src/types/**',
      ],
      thresholds: {
        branches: 80,
        functions: 80,
        lines: 80,
        statements: 80,
      },
    },
    testTimeout: 10000,
    hookTimeout: 10000,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

#### 验证
- [x] `vitest.config.ts` 文件已创建
- [x] 测试命令可以正常运行

---

### 1.8 配置 Git Hooks

#### 目标
配置 Husky 和 lint-staged，实现提交前自动检查。

#### 初始化 Husky
```bash
npx husky install
```

#### 创建 `.husky/pre-commit`
```bash
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npx lint-staged
```

#### 配置 lint-staged（在 package.json 中）
```json
{
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.json": [
      "prettier --write"
    ]
  }
}
```

#### 验证
- [x] `.husky` 目录已创建
- [x] `pre-commit` hook 已配置
- [x] 提交时自动运行 lint-staged

---

### 1.9 创建主入口文件

#### 目标
创建 CLI 主入口文件框架。

#### 创建 `src/index.ts`
```typescript
#!/usr/bin/env node

import { Command } from 'commander';
import chalk from 'chalk';

const program = new Command();

program
  .name('codex-sessions')
  .description('Codex Session Viewer - 查看和管理 Codex CLI 的历史 session 记录')
  .version('1.0.0');

// TODO: 注册命令
// program.addCommand(listCommand);
// program.addCommand(viewCommand);
// program.addCommand(searchCommand);
// program.addCommand(exportCommand);
// program.addCommand(tuiCommand);

// 错误处理
program.exitOverride();

try {
  program.parse();
} catch (error: unknown) {
  if (error instanceof Error) {
    if (error.message.includes('help')) {
      program.help();
    } else if (error.message.includes('version')) {
      console.log(chalk.blue('codex-sessions v1.0.0'));
    } else {
      console.error(chalk.red('错误:'), error.message);
      process.exit(1);
    }
  } else {
    console.error(chalk.red('未知错误:'), error);
    process.exit(1);
  }
}

// 如果没有提供命令，显示帮助信息
if (!process.argv.slice(2).length) {
  program.outputHelp();
}
```

#### 验证
- [x] `src/index.ts` 文件已创建
- [x] 文件语法正确
- [x] 可以正常编译

---

### 1.10 配置版本控制

#### 目标
配置 Git 版本控制。

#### 创建 `.gitignore`
```gitignore
# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
build/
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Testing
coverage/
.nyc_output/

# Misc
*.tsbuildinfo
*.tgz
.cache/
.parcel-cache/

# Temporary files
tmp/
temp/
```

#### 初始化 Git 仓库
```bash
git init
git add .
git commit -m "feat: 初始化项目结构"
```

#### 验证
- [x] `.gitignore` 文件已创建
- [x] Git 仓库已初始化
- [x] 初始提交成功

---

## 完成标准

- [x] npm 项目已初始化
- [x] 所有依赖已安装
- [x] TypeScript 配置完成
- [x] 项目目录结构已创建
- [x] 代码质量工具配置完成
- [x] 构建工具配置完成
- [x] 测试框架配置完成
- [x] Git Hooks 配置完成
- [x] 主入口文件已创建
- [x] Git 版本控制配置完成

## 下一步

完成本文档后，继续进行 [02-TypeScript类型定义](./02-TypeScript类型定义.md)。